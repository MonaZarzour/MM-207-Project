meta {
  name: Edit Account
  type: http
  seq: 10
}

put {
  url: {{baseUrl}}/users/me
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  { "displayName": "Mona" }
}

settings {
  encodeUrl: true
  timeout: 0
}
